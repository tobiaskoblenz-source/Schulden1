function modal(html){$('modal').innerHTML=html;$('modalBack').classList.add('show');$('modal').querySelector('[data-close-modal]')?.addEventListener('click',closeModal)}function closeModal(){$('modalBack').classList.remove('show')}
function formHtml(d={},title='Neue Schuld'){const cats=readCats(),c=d.contactDetails||{};return`<div class="modalHead"><div><h2>${esc(title)}</h2><p>Die wichtigen Daten an einem Ort.</p></div><button class="closeBtn" data-close-modal>×</button></div><form id="debtForm"><div class="formGrid"><label>Gläubiger<input class="field" name="name" required value="${esc(d.name||'')}"></label><label>Grund<input class="field" name="grund" required value="${esc(d.grund||'')}"></label><label>Betrag (€)<input class="field" name="betrag" inputmode="decimal" required value="${esc(d.betrag??d.amount??'')}"></label><label>Datum<input class="field" type="date" name="datum" value="${esc(d.datum||d.date||'')}"></label><label>Aktenzeichen<input class="field" name="aktenzeichen" value="${esc(d.aktenzeichen||d.caseNumber||'')}"></label><label>Kategorie<select name="category"><option value="">– auswählen –</option>${cats.map(x=>`<option ${categoryOf(d)===x?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label>Status<select name="status"><option value="offen" ${!isClosed(d.status)?'selected':''}>Offen</option><option value="erledigt" ${isClosed(d.status)?'selected':''}>Erledigt</option></select></label><label>Ansprechpartner<input class="field" name="representative" value="${esc(c.representative||'')}"></label><label class="span2">Anschrift<input class="field" name="address" value="${esc(c.address||'')}"></label><label>E-Mail<input class="field" type="email" name="email" value="${esc(c.email||'')}"></label><label>Telefon<input class="field" name="phone" value="${esc(c.phone||'')}"></label>${d.__new?'<label class="span2">Erste Notiz<textarea name="firstNote" placeholder="optional"></textarea></label>':''}</div><div class="modalActions"><button class="btn" type="button" data-close-modal>Abbrechen</button><button class="btn primary" type="submit">Speichern</button></div></form>`}
function parseMoney(v){let s=String(v??'').trim().replace(/\s/g,'');if(s.includes(',')&&s.includes('.'))s=s.replace(/\./g,'').replace(',','.');else if(s.includes(','))s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:NaN}
function openAdd(prefill=''){const d={__new:true,name:prefill,datum:new Date().toISOString().slice(0,10),status:'offen'};modal(formHtml(d,'Neue Schuld'));$('debtForm').onsubmit=e=>{e.preventDefault();const fd=new FormData(e.currentTarget),betrag=parseMoney(fd.get('betrag'));if(!fd.get('name')||!fd.get('grund')||!Number.isFinite(betrag)||betrag<=0){toast('Bitte Name, Grund und Betrag prüfen.');return}const note=String(fd.get('firstNote')||'').trim();state.debts.push({name:String(fd.get('name')).trim(),grund:String(fd.get('grund')).trim(),betrag,datum:String(fd.get('datum')||''),aktenzeichen:String(fd.get('aktenzeichen')||'').trim(),category:String(fd.get('category')||'').trim(),status:String(fd.get('status')||'offen'),contactDetails:{representative:String(fd.get('representative')||'').trim(),address:String(fd.get('address')||'').trim(),email:String(fd.get('email')||'').trim(),phone:String(fd.get('phone')||'').trim()},notesHistory:note?[{at:new Date().toISOString(),text:note}]:[],history:[{at:new Date().toISOString(),text:'Schuld angelegt'}],correspondence:[],paperlessLinks:[]});closeModal();commit('Schuld gespeichert')}}
function openEdit(i){const d=state.debts[i];if(!d)return;modal(formHtml(d,'Schuld bearbeiten'));$('debtForm').onsubmit=e=>{e.preventDefault();const fd=new FormData(e.currentTarget),betrag=parseMoney(fd.get('betrag'));if(!fd.get('name')||!fd.get('grund')||!Number.isFinite(betrag)||betrag<0){toast('Bitte Eingaben prüfen.');return}Object.assign(d,{name:String(fd.get('name')).trim(),grund:String(fd.get('grund')).trim(),betrag,datum:String(fd.get('datum')||''),aktenzeichen:String(fd.get('aktenzeichen')||'').trim(),category:String(fd.get('category')||'').trim(),status:String(fd.get('status')||'offen'),contactDetails:{representative:String(fd.get('representative')||'').trim(),address:String(fd.get('address')||'').trim(),email:String(fd.get('email')||'').trim(),phone:String(fd.get('phone')||'').trim()}});if(!Array.isArray(d.history))d.history=[];d.history.push({at:new Date().toISOString(),text:'Schuld bearbeitet'});closeModal();commit('Änderungen gespeichert',true)}}
function openPayment(i){const d=state.debts[i];if(!d)return;modal(`<div class="modalHead"><div><h2>Zahlung eintragen</h2><p>${esc(nameOf(d))} · aktuell ${eur.format(amount(d))}</p></div><button class="closeBtn" data-close-modal>×</button></div><form id="paymentForm"><div class="formGrid"><label>Zahlung (€)<input class="field" name="pay" inputmode="decimal" required></label><label>Datum<input class="field" type="date" name="date" value="${new Date().toISOString().slice(0,10)}"></label><label class="span2">Notiz<input class="field" name="note" placeholder="optional"></label></div><div class="modalActions"><button class="btn" type="button" data-close-modal>Abbrechen</button><button class="btn good" type="submit">Zahlung speichern</button></div></form>`);$('paymentForm').onsubmit=e=>{e.preventDefault();const fd=new FormData(e.currentTarget),p=parseMoney(fd.get('pay'));if(!Number.isFinite(p)||p<=0){toast('Zahlung prüfen.');return}d.betrag=Math.max(0,amount(d)-p);if(!Array.isArray(d.history))d.history=[];d.history.push({at:new Date().toISOString(),text:`Zahlung ${eur.format(p)}${fd.get('note')?' · '+fd.get('note'):''}`});if(d.betrag===0)d.status='erledigt';closeModal();commit('Zahlung gespeichert',true)}}
function openNote(i){const d=state.debts[i];if(!d)return;modal(`<div class="modalHead"><div><h2>Notiz hinzufügen</h2><p>${esc(nameOf(d))}</p></div><button class="closeBtn" data-close-modal>×</button></div><form id="noteForm"><label class="small">Notiz<textarea name="note" required autofocus></textarea></label><div class="modalActions"><button class="btn" type="button" data-close-modal>Abbrechen</button><button class="btn primary" type="submit">Speichern</button></div></form>`);$('noteForm').onsubmit=e=>{e.preventDefault();const t=String(new FormData(e.currentTarget).get('note')||'').trim();if(!t)return;if(!Array.isArray(d.notesHistory))d.notesHistory=[];d.notesHistory.push({at:new Date().toISOString(),text:t});d.notes=(d.notes?d.notes+'\n':'')+t;closeModal();commit('Notiz gespeichert',true)}}
function toggleStatus(i){const d=state.debts[i];if(!d)return;d.status=isClosed(d.status)?'offen':'erledigt';if(!Array.isArray(d.history))d.history=[];d.history.push({at:new Date().toISOString(),text:isClosed(d.status)?'Als erledigt markiert':'Wieder geöffnet'});commit('Status geändert',true)}
function deleteDebt(i){const d=state.debts[i];if(!d)return;modal(`<div class="modalHead"><div><h2>Schuld löschen?</h2><p>${esc(nameOf(d))} · ${eur.format(amount(d))}</p></div><button class="closeBtn" data-close-modal>×</button></div><p class="muted">Der Eintrag landet im Papierkorb und kann wiederhergestellt werden.</p><div class="modalActions"><button class="btn" data-close-modal>Abbrechen</button><button class="btn danger" id="deleteConfirm">In Papierkorb</button></div>`);$('deleteConfirm').onclick=()=>{const trash=readTrash();trash.push({...d,__deletedAt:new Date().toISOString()});writeTrash(trash);state.debts.splice(i,1);closeModal();closeDrawer();commit('In Papierkorb verschoben')}}
function openCategories(){const cats=readCats();modal(`<div class="modalHead"><div><h2>Kategorien</h2><p>Für Gläubiger und Schulden.</p></div><button class="closeBtn" data-close-modal>×</button></div><div id="catList" style="display:grid;gap:8px">${cats.map((x,i)=>`<div class="settingRow"><div class="left"><strong>${esc(x)}</strong></div><button class="btn danger" data-cat-del="${i}">Löschen</button></div>`).join('')}</div><form id="catForm" class="row" style="margin-top:12px"><input class="field" name="cat" placeholder="Neue Kategorie"><button class="btn primary">Hinzufügen</button></form>`);$('catForm').onsubmit=e=>{e.preventDefault();const x=String(new FormData(e.currentTarget).get('cat')||'').trim();if(x&&!cats.some(c=>c.toLowerCase()===x.toLowerCase())){cats.push(x);writeCats(cats);openCategories()}};$('catList').onclick=e=>{const b=e.target.closest('[data-cat-del]');if(!b)return;cats.splice(Number(b.dataset.catDel),1);writeCats(cats);openCategories()}}
function openTrash(){const tr=readTrash();modal(`<div class="modalHead"><div><h2>Papierkorb</h2><p>${tr.length} gelöschte Einträge</p></div><button class="closeBtn" data-close-modal>×</button></div><div style="display:grid;gap:8px">${tr.map((d,i)=>`<div class="settingRow"><div class="left"><strong>${esc(nameOf(d))} · ${eur.format(amount(d))}</strong><span>${esc(reasonOf(d))}</span></div><button class="btn good" data-restore="${i}">Wiederherstellen</button></div>`).join('')||'<div class="empty">Papierkorb ist leer.</div>'}</div>`);$('modal').onclick=e=>{const b=e.target.closest('[data-restore]');if(!b)return;const i=Number(b.dataset.restore),d=tr.splice(i,1)[0];if(d){delete d.__deletedAt;state.debts.push(d);writeTrash(tr);openTrash();commit('Eintrag wiederhergestellt')}}}